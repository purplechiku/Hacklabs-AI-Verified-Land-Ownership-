{
  "name": "landregistry-wrapper-service",
  "version": "1.0.0",
  "description": "HTTP wrapper service exposing /register and /ownership/:plotNumber. Backend calls this over HTTP so it never needs a wallet or private key directly.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.2",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
